<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.ActionBar" />

    <style name="SearchViewTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorControlActivated">?colorAccent</item>
        <item name="searchViewStyle">@style/Widget.Moe.SearchView</item>
    </style>

    <style name="Widget.Moe.SearchView" parent="Widget.AppCompat.SearchView">
        <item name="searchIcon">@android:color/transparent</item>
        <item name="searchHintIcon">@android:color/transparent</item>
        <item name="queryBackground">@android:color/transparent</item>
        <item name="submitBackground">@android:color/transparent</item>
    </style>

</resources>
